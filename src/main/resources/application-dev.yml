server:
  port: 8080
spring:
  thymeleaf:
    cache: false
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1:3306/rest_dev
    username: root
    password: